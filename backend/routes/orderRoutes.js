const express = require('express');
const router = express.Router();

// Placeholder for order endpoints
router.get('/', (req, res) => {
  res.json({ message: 'orders route placeholder' });
});

module.exports = router;